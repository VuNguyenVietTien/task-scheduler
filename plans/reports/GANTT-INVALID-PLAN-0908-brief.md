# GANTT-INVALID-PLAN-0908
Role implementer/debugger. Route Pi openai-codex/gpt-5.6-sol high. Windows2026-09-08 Asia/Bangkok, pane w1:p42. Worktree C:/Users/TienVNV/Documents/prjmngr/worktrees/gantt-invalid-plan-0908 branch codex/gantt-invalid-plan-0908 base11ebd45. Terminal starts dev-0908; product edits only assigned worktree.
Read AGENTS and required current docs; bounded code hooks/usePlanLifecycle.ts, timeline/PlanLifecycleBar.tsx, utils/planLifecycle.ts and owning tests, GANTT-PLAN-UX-0908.md. Load only relevant debug skill; avoid broad exploration/review/ship workflow. User wants fast patch unit only.
Evidence: PM Chrome postcheck newest r2 falls back No plan but still raw plan_data.tasks[0] has invalid daily hours. Previous parser fix handles missing hours only; this is supplied invalid vector. Current failed load loses plan identity, so user cannot delete corrupt plan via toolbar. User original request invalid daily hours when deleting plan must be handled.
Own lifecycle hook+toolbar+focused tests+locales. No other active code workers. Preserve zero-effort milestones, scheduling, immutable snapshots, authorization. Do not alter backend/DB, do not weaken supplied vector validation or invent allocations. No production test mutation.
Acceptance: unreadable selected/newest plan retains identifiable metadata as failed selection and accessible existing Delete plan button/confirmation; displayed timeline safely current scheduling with explicit localized notice. Hide raw parser details from normal UI. Delete successfully removes selected corrupt plan via existing API, transitions newest remaining/no-plan without reparsing removed snapshot. Explicit No plan clears failed identity/message. Valid plan defaults unchanged. Add small unit cases corrupt initial/manual selection -> delete available -> fallback transition, no-plan clear; no broad test runs.
Optional diagnose vector format only from available safe evidence; don't spend on live credentials/DB inspection. Report data remains invalid and UI recovery fixed, never say snapshot repaired. Commit own code/report; PM integrates and pushes. Existing dev node_modules for Jest; use NODE_PATH and reporter default; no installs.
Target10min. Report own plans/reports/GANTT-INVALID-PLAN-0908.md exact commits/tests/root cause. No ledger writes. Finish Status DONE/DONE_WITH_CONCERNS/BLOCKED/NEEDS_CONTEXT and report absolute path.
