# PM GraphQL Integrate 2 — Task 1.3 completion (authz hardening + Task phase/category mounting)

**Session:** takeover of stalled Increment 1 Task 1.3 (prior GLM pane stalled twice in built-in editor) · **Date:** 2026-09-04
**Scope owned:** Task 1.3 backend GraphQL/schema/projection/contract paths + the two web GraphQL contract fixture tests + this report. No Timeline/UI, no migrations, no plan resolvers touched. No worktree/clone/branch/commit/push/deploy/credentials/env/live-DB. Shared checkout: all pre-existing dirty/untracked work preserved; only atomic `apply_patch`/single-file perl patches used.

---

## 1. What landed before takeover (verified, NOT recreated)

| Artifact | State found | Evidence |
|---|---|---|
| `project_authz` inline module | complete | `src/graphql/resolvers/mod.rs:23-100` — `PROJECT_WRITE_ROLE_SQL` (owner OR manager/leader/admin), `PROJECT_READ_MEMBER_SQL` (owner OR any member), `require_user`, `require_project_write`, `require_project_read` |
| Taxonomy guards | complete | `taxonomies/mod.rs`: 5 write guards (ensure/create/update/archive/set_task_taxonomy) + 1 read (project_phases), all pre-write |
| Resource member guards (partial) | 1 of 3 writes, 2 of 2 reads | create/list/single guarded; `link_resource_member_user` and `classify_resource_member` had NO guard |
| Projection read guard | missing | `project_schedule_projection` had no auth |
| RED auth tests | landed, RED | `tests/contract/increment1_graphql.rs` (mtime Sep 4 22:34): 5 authz tests incl. `authz_guards_are_wired_into_every_scheduling_resolver` (exact substring counts: taxonomies 5W/1R, resource_members 3W/2R, schedule_projection 0W/1R) + unauthenticated-before-DB behavioral tests |
| Prior review | pm-graphql-review-1 ACCEPT (Sep 2) | contract surface + projector math already accepted; this session closes its F1 follow-up (authorization) which superseded that review's "no extra auth" limitation |

## 2. RED evidence (this session, before patches)

`cd backend && cargo test --test contract authz` → **FAILED. 2 passed; 3 failed; exit 1**:
- `authz_guards_are_wired_into_every_scheduling_resolver`: taxonomies 6 write-matches vs expected 5 (name-import counted; test pins fully-qualified-call style)
- `authz_projection_and_taxonomy_resource_reads_require_login`: `project_schedule_projection` hit DB ("pool timed out…") before auth gate
- `authz_unauthenticated_resource_member_writes_are_rejected_before_db`: `link_resource_member_user` hit DB before auth gate

## 3. Patches applied (atomic, one file per step)

| File | Change |
|---|---|
| `resolvers/taxonomies/mod.rs` | name-import → `use …project_authz;`; 6 call sites fully-qualified → exactly 5W/1R matches |
| `resolvers/resource_members/mod.rs` | same import collapse + qualification; `link_resource_member_user`: `require_user` first (renamed `caller_id` to avoid shadowing the `user_id` arg), `require_project_write` after FOR UPDATE project resolution, before any link write; `classify_resource_member`: `require_user` + `require_project_write(member.project_id)` after fetch, before INSERT → exactly 3W/2R |
| `resolvers/schedule_projection/mod.rs` | `require_user` + `require_project_read` before the 3 projection queries → exactly 0W/1R. `import_dry_run` stays pure/login-free-by-design (no project data; test pins 0 writes for this file) |
| `types/task.rs` | **mounted `Task.phase_id/category_id: Option<Uuid>`** (nullable = Unphased/uncategorised) — was missing from SDL despite mandate |
| `resolvers/tasks/mutation/{create,reorder,update,update_effort,update_status}.rs` + `query/{task,tasks,task_subtasks}.rs` | all 9 `Task` struct literals now set `phase_id/category_id` via `row.get(...)`; all source SQL is `SELECT t.*`/`RETURNING *` so columns always present. (One perl misfire added the fields to a `ForestRow` literal in `query/tasks.rs` — reverted immediately; compiler-verified) |
| `schema.graphql` | regenerated by the suite's deterministic SDL test; `type Task` now exposes `phase_id: UUID` / `category_id: UUID` |

## 4. GREEN evidence (exact commands & results, this session)

```
cd backend
cargo test --test contract            → test result: ok. 28 passed; 0 failed; 0 ignored   [exit 0]
                                         (23 prior Increment-1 contract tests + 5 new authz)
cargo test --test taxonomy_hierarchy  → test result: ok. 5 passed; 0 failed; 1 ignored   [exit 0]
                                         (1 ignored = intentional live-PG suite)
cargo check --all-targets             → Finished `dev` profile … 0 errors              [exit 0]
                                         (pre-existing unused-import warnings only)
cd ../web
npx jest src/graphql/__tests__/w3-contract.test.ts src/graphql/__tests__/w3-sdl-fixture.test.ts \
  --config jest.config.js --reporters default --runInBand
                                       → Test Suites: 2 passed, 2 total
                                         Tests: 43 passed, 43 total                     [exit 0]
                                         (`--reporters default` = documented dual-config workaround)
```

## 5. Invariants now locked (test-pinned)

1. **Every taxonomy/resource mutation requires project write role** — owner OR manager/leader/admin; unauthenticated writes rejected BEFORE any DB touch (behavioral tests + exact guard-count test).
2. **Resource/taxonomy/projection reads require project membership** — owner OR any `project_members` row; `project_schedule_projection`, `resource_members`, `resource_member`, `project_phases`.
3. **Related IDs same-project** — `set_task_taxonomy` → `taxonomy::validate_term_for_project`; `classify` → domain `validate_classification` (same-project, MEMBER target, COMPANY/GROUP classifier); `create` scoped by guarded project_id; `link`'s `user_id` is deliberately global (Task 1.2 accepted spec: cross-project user linking allowed).
4. **`CURRENT_TASK_FIELDS` is the sole projection source** — `sdl_has_no_schedule_engine_dependency` scans operative SDL (doc-stripped) for allocation/segment/meeting/capacity/`CAPACITY_SCHEDULER`.
5. **Exact-once rollups** — `projection_counts_each_real_task_once_regardless_of_depth`.
6. **Unknown/inactive phase → Unphased** — never dropped; Unphased group always appended last.
7. **Source headings are not tasks** — `sdl_source_heading_is_distinct_with_no_task_identity` (no task_id/dates/effort/progress on `ScheduleSourceHeading`).
8. **Task phase/category fields mounted** — NEW this session: SDL `Task.phase_id/category_id: UUID`; deterministic regen green; web fixtures synchronized (43/43).
9. **Deterministic SDL** — `regenerates_schema_dot_graphql` re-run green post-change.

## 6. Frontend SDL handoff (Task 1.4)

- `type Task { … phase_id: UUID; category_id: UUID }` — both nullable; NULL = Unphased/uncategorised. Read via existing task queries (`SELECT t.*` already returns them).
- Phase assignment: `mutation set_task_taxonomy(task_id: ID!, phase_id: ID, category_id: ID): Boolean!` — writes need manager-or-owner role.
- Phases list: `query project_phases(project_id: ID!): [ProjectPhaseType!]!` (read = membership).
- Projection: `query project_schedule_projection(project_id: ID!): ProjectScheduleProjection!` with `source: ScheduleProjectionSource!` (only `CURRENT_TASK_FIELDS`), `ScheduleWbsRow = ScheduleTaskEntry | ScheduleSourceHeading`, `SchedulePhaseGroup { phase_id, phase_key, is_unphased, task_ids, totals }`, `ScheduleTotals.effort_hours: String!` (2-dp decimal string). Weighted progress: contributors = tasks with progress; weight = effort>0?effort:1.0 (e.g. (10×100+20×50)/30 = 66.67).
- Jest fixture tests read the LIVE `backend/schema.graphql` — no stale-copy risk; they are green against the regenerated SDL.

## 7. Risks / notes

- Resolver DB-path execution (auth queries against real Postgres) still deferred to Task 1.5 e2e, same layering as 1.1/1.2 handoffs; behavioral tests here prove gate ordering (before-DB) via dead-pool timeout signature.
- Guard-count test pins a fully-qualified-call style in the three resolver files — future edits must keep `use` of guard names out of those files or counts drift (intentional tripwire, matches prior owner's design).
- Prior review P2s remain open for Increment 2: strict effort/progress column reads (`mod.rs` lenient `.ok().flatten()`), task-cycle members not surfaced as WBS roots, sum-then-round in rollup, §5.3 wording reconciliation.
- `schema.graphql` diff vs HEAD is large because it carries all prior uncommitted sessions' accumulation; this session's delta is solely the regenerated SDL including the two new Task fields.

## 8. Verdict

**READY_FOR_REVIEW** — all mandated invariants implemented and test-locked; RED→GREEN evidence recorded; full focused gate matrix green (28+5 backend, 43 web, check clean); no out-of-scope file touched; shared-checkout work preserved.
